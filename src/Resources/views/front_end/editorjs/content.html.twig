<{{ wrapper_tag }} class="editorjs-content" data-update-at="{{ content.time }}" data-version="{{ content.version }}">
    {% for blockData in blocks %}
        {% include "@WebToolkit/front_end/editorjs/blocks/#{blockData.type}.html.twig" with {
            starting_heading_level: starting_heading_level,
            block: blockData.block,
        } %}
    {% endfor %}
</{{ wrapper_tag }}>